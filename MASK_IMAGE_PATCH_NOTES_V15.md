# v15 蒙版与图片保真修复

新增两个导出选项：

- **蒙版转 Frame**：默认开启。将带子图层的矩形/圆角蒙版导出为 Figma Frame，并开启 clipsContent，避免蒙版本身的白色/背景填充在 Figma 中不可见。
- **图片保真 SVG**：默认关闭。把带图片填充的图层整体用 SVG 兜底导出，适合 MasterGo 中被拉伸过的图片，优先保证视觉比例；代价是该图片层可能合并。

保留 v14 的手动调节窗口尺寸、v13 图标转 SVG、v9 不合并图层颜色继承逻辑。
