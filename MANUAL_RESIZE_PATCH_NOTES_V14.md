# v14 手动调节插件窗口尺寸

本版本新增插件窗口尺寸调节能力。

## 新增

- 导出端与导入端均新增右上角「窗口 小 / 中 / 大」快捷尺寸按钮。
- 导出端与导入端均新增右下角拖拽手柄，可拖拽调整插件窗口宽高。
- 调整后的尺寸会保存在 localStorage，下次打开插件会尝试恢复。
- 原有自动 resize 改为“只增大不缩小”，避免切换传输方式或导入文件时覆盖用户手动设置的窗口尺寸。

## 使用

1. 点击右上角「小 / 中 / 大」快速切换窗口大小。
2. 或按住右下角斜纹拖拽手柄，手动拖大/缩小窗口。
3. 小屏幕下建议先点「小」，再根据需要拖拽。
