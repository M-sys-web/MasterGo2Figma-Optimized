# v10 底部按钮固定修复

修复 MasterGo 导出端在窗口高度较小、页面列表或状态卡片内容较多时，底部「开始」按钮被挤出可视区域的问题。

## 调整

- 将「开始」按钮从 sticky 改为 fixed，固定在插件窗口底部。
- 为主滚动区增加底部留白，避免内容被按钮遮挡。
- 增加底部渐变遮罩，确保按钮在滚动内容上方可读。

## 影响范围

仅修改 `SendToFigma/ui.html` 的布局样式，不影响导出逻辑、颜色继承逻辑或 Figma 导入端逻辑。
